import React from "react";
import { render } from "@testing-library/react";
import { {{PascalCase}} } from "./{{camelCase}}";

test("should...", () => {
  render(<{{PascalCase}} />);
  const = 'This is the Original template'
});
